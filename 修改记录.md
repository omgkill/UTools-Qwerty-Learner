# 修改记录

## 2026-02-14 移除循环发音开关功能

### 修改目的
移除循环发音开关功能，简化发音设置。

### 修改内容

#### 1. 移除 Store 中的 isLoop 字段
**文件**: `src/store/index.ts`

**修改内容**:
- 从 `pronunciationConfigAtom` 中移除 `isLoop` 字段

#### 2. 移除 PronunciationSwitcher 中的循环发音开关 UI
**文件**: `src/pages/Typing/components/PronunciationSwitcher/index.tsx`

**修改内容**:
- 移除 `onChangePronunciationIsLoop` 函数
- 移除循环发音开关 UI 组件

#### 3. 简化 usePronunciation hook
**文件**: `src/hooks/usePronunciation.ts`

**修改内容**:
- 移除 `isLoop` 参数
- 移除 `loop` 状态计算逻辑
- 移除动态更新循环状态的 `useEffect`
- `loop` 固定为 `false`

#### 4. 更新调用方
**文件**:
- `src/pages/Typing/components/ResultScreen/WordChip.tsx`
- `src/pages/Typing/components/ResultScreen/MiniWordChip.tsx`

**修改内容**:
- 移除 `usePronunciationSound` 的第二个参数

### 修改后结果
- 发音不再支持循环播放
- 移除循环发音开关 UI

---

## 2026-02-14 移除深色模式开关，只保留深色模式

### 修改目的
移除深色模式开关功能，应用始终使用深色模式，删除浅色模式相关代码和资源。

### 修改内容

#### 1. 移除 Switcher 组件中的深色模式开关按钮
**文件**: `src/pages/Typing/components/Switcher/index.tsx`

**修改内容**:
- 移除 `isOpenDarkModeAtom` 导入
- 移除 `useAtom` 导入
- 移除 `IconMoon`、`IconSun` 图标导入
- 移除 `changeDarkModeState` 函数
- 移除 `Ctrl+D` 快捷键绑定
- 移除深色模式开关按钮 UI

#### 2. 修改 index.tsx
**文件**: `src/index.tsx`

**修改前**:
- 监听 `isOpenDarkModeAtom` 状态变化
- 根据状态动态添加/移除 `dark` class

**修改后**:
- 移除 `isOpenDarkModeAtom` 导入和 `useAtomValue` 导入
- 在 `useEffect` 中直接添加 `dark` class

```typescript
useEffect(() => {
  document.documentElement.classList.add('dark')
}, [])
```

#### 3. 修改 public/index.html
**文件**: `public/index.html`

**修改内容**:
- 在 `<html>` 标签上直接添加 `class="dark"`
- 移除深色模式初始化脚本

#### 4. 移除 Store 状态
**文件**: `src/store/index.ts`

**移除内容**:
- `isOpenDarkModeAtom` 原子状态

#### 5. 更新 mixpanel.ts
**文件**: `src/utils/mixpanel.ts`

**修改内容**:
- 移除 `isOpenDarkModeAtom` 导入
- `modeDark` 字段固定为 `true`

#### 6. 更新 index.css
**文件**: `src/index.css`

**修改内容**:
- body 默认背景色改为 `bg-gray-900`
- 移除 `html.dark body` 条件样式
- 将所有 `dark:` 前缀的样式改为默认样式

#### 7. 更新各组件
**文件**:
- `src/pages/Analysis/index.tsx`
- `src/pages/Analysis/components/HeatmapCharts.tsx`
- `src/pages/Analysis/components/LineCharts.tsx`

**修改内容**:
- 移除 `isOpenDarkModeAtom` 导入和使用
- 将 `dark:` 样式改为默认样式
- 图表主题固定为深色主题

### 修改后结果
- 应用始终处于深色模式
- 移除深色模式开关按钮和 Ctrl+D 快捷键
- 移除 `isOpenDarkModeAtom` 状态
- 简化 CSS 样式，无需条件判断

---

## 2026-02-14 移除音效设置功能

### 修改目的
移除音效设置功能，默认没有音效，简化应用。

### 修改内容

#### 1. 删除 SoundSwitcher 组件
**文件**: `src/pages/Typing/components/SoundSwitcher/index.tsx`

**删除内容**:
- 整个组件文件（包含按键音和效果音的快速开关 Popover）

#### 2. 更新 Switcher 组件
**文件**: `src/pages/Typing/components/Switcher/index.tsx`

**修改内容**:
- 移除 `SoundSwitcher` 组件导入
- 移除工具栏中的音效设置按钮

#### 3. 简化 SoundSetting 组件
**文件**: `src/pages/Typing/components/Setting/SoundSetting.tsx`

**修改前**:
- 包含单词发音、释义发音、按键音、效果音四个设置区块

**修改后**:
- 只保留单词发音和释义发音设置
- 移除按键音和效果音相关代码

#### 4. 更新 Word 组件
**文件**: `src/pages/Typing/components/WordPanel/components/Word/index.tsx`

**修改内容**:
- 移除 `useKeySounds` hook 导入和调用
- 移除 `playKeySound`、`playBeepSound`、`playHintSound` 调用

#### 5. 删除 useKeySounds hook
**文件**: `src/hooks/useKeySounds.ts`

**删除内容**:
- 整个 hook 文件（封装按键音、错误音、正确音的播放逻辑）

#### 6. 删除按键音播放工具
**文件**: `src/utils/sounds/keySounds.ts`

**删除内容**:
- 整个文件（使用 Howler 库播放按键音）

#### 7. 移除 Store 状态
**文件**: `src/store/index.ts`

**移除内容**:
- `keySoundsConfigAtom` 原子状态（按键音配置）
- `hintSoundsConfigAtom` 原子状态（效果音配置）
- 相关资源导入

#### 8. 简化 soundResource.ts
**文件**: `src/resources/soundResource.ts`

**移除内容**:
- `SOUND_URL_PREFIX` 常量
- `KEY_SOUND_URL_PREFIX` 常量
- `keySoundResources` 资源列表
- `wrongSoundResources` 资源列表
- `correctSoundResources` 资源列表

**保留内容**:
- `LANG_PRON_MAP` 语言发音映射（用于单词发音功能）

#### 9. 删除音频资源文件
**目录**: `public/sounds/`

**删除内容**:
- `key-sound/` 整个目录（14个机械键盘音效文件）
- `beep.wav` 错误提示音
- `click.wav` 点击音
- `correct.wav` 正确提示音

### 保留功能
- 单词发音功能（通过 `pronunciationConfigAtom` 配置）
- 释义发音功能

### 移除功能
- 按键音功能（机械键盘音效）
- 效果音功能（错误提示音、正确提示音）
- 音效快速开关（工具栏按钮）

---

## 2026-02-14 移除单个单词循环功能

### 修改目的
移除单个单词循环功能，简化应用功能。

### 修改内容

#### 1. 删除 LoopWordSwitcher 组件
**文件**: `src/pages/Typing/components/LoopWordSwitcher/index.tsx`

**删除内容**:
- 整个组件文件（包含 Popover 弹出框和循环次数选择器）

#### 2. 更新 Switcher 组件
**文件**: `src/pages/Typing/components/Switcher/index.tsx`

**修改内容**:
- 移除 `LoopWordSwitcher` 组件导入
- 移除工具栏中的循环按钮 UI

#### 3. 简化 WordPanel 组件
**文件**: `src/pages/Typing/components/WordPanel/index.tsx`

**修改前**:
- 包含 `currentWordExerciseCount` 状态
- 包含 `loopWordTimes` 配置读取
- `onFinish` 回调中有循环判断逻辑

**修改后**:
- 移除循环相关状态和逻辑
- `onFinish` 回调简化为直接进入下一个单词或完成章节

```typescript
const onFinish = useCallback(() => {
  if (state.chapterData.index < state.chapterData.words.length - 1) {
    dispatch({ type: TypingStateActionType.NEXT_WORD })
  } else {
    dispatch({ type: TypingStateActionType.FINISH_CHAPTER })
  }
}, [state.chapterData.index, state.chapterData.words.length, dispatch])
```

#### 4. 移除 Store 状态
**文件**: `src/store/index.ts`

**移除内容**:
- `loopWordConfigAtom` 原子状态
- `LoopWordTimesOption` 类型导入

#### 5. 移除类型定义
**文件**: `src/typings/index.ts`

**移除内容**:
- `LoopWordTimesOption` 类型定义

#### 6. 简化 Typing Store
**文件**: `src/pages/Typing/store/index.ts`

**移除内容**:
- `isLoopSingleWord` 状态字段
- `LOOP_CURRENT_WORD` action 类型
- `SET_IS_LOOP_SINGLE_WORD` action 类型
- `TOGGLE_IS_LOOP_SINGLE_WORD` action 类型
- 相关的 reducer 处理逻辑

### 移除功能
- 单个单词循环功能（1/3/5/8/无限次循环）

---

## 2026-02-14 简化默写模式

### 修改目的
简化卡关默写模式，移除复杂的选项配置，点击默写按钮直接开启全部隐藏模式。

### 修改内容

#### 1. WordDictationSwitcher 组件
**文件**: `src/pages/Typing/components/WordDictationSwitcher/index.tsx`

**修改前**:
- 使用 Popover 弹出框显示设置面板
- 包含 Switch 开关和 Listbox 选择器
- 支持四种隐藏模式：全部隐藏、隐藏元音、隐藏辅音、随机隐藏

**修改后**:
- 移除 Popover、Listbox、Switch 等组件
- 点击按钮直接切换默写模式开关
- 只保留全部隐藏模式
- 保留 Ctrl+V 快捷键

#### 2. 类型定义
**文件**: `src/typings/index.ts`

**移除内容**:
- `WordDictationType` 类型定义
- `WordDictationOpenBy` 类型定义及相关注释

#### 3. Store 状态管理
**文件**: `src/store/index.ts`

**修改前**:
```typescript
export const wordDictationConfigAtom = atomForConfig('wordDictationConfig', {
  isOpen: false,
  type: 'hideAll' as WordDictationType,
  openBy: 'auto' as WordDictationOpenBy,
})
```

**修改后**:
```typescript
export const wordDictationConfigAtom = atomForConfig('wordDictationConfig', {
  isOpen: false,
})
```

#### 4. Word 组件
**文件**: `src/pages/Typing/components/WordPanel/components/Word/index.tsx`

**修改内容**:
- 移除 `vowelLetters` 常量
- 移除 `randomLetterVisible` 状态字段
- 简化 `getLetterVisible` 函数，只保留全部隐藏逻辑

**修改前**:
```typescript
const getLetterVisible = useCallback((index: number) => {
  if (wordState.letterStates[index] === 'correct' || (isShowAnswerOnHover && isHoveringWord)) return true

  if (wordDictationConfig.isOpen) {
    if (wordDictationConfig.type === 'hideAll') return false
    // ... 其他隐藏模式逻辑
  }
  return true
}, [...])
```

**修改后**:
```typescript
const getLetterVisible = useCallback((index: number) => {
  if (wordState.letterStates[index] === 'correct' || (isShowAnswerOnHover && isHoveringWord)) return true

  if (wordDictationConfig.isOpen) {
    return false
  }
  return true
}, [isHoveringWord, isShowAnswerOnHover, wordDictationConfig.isOpen, wordState.letterStates])
```

#### 5. ResultScreen 组件
**文件**: `src/pages/Typing/components/ResultScreen/index.tsx`

**修改内容**:
- 移除 `repeatButtonHandler` 和 `nextButtonHandler` 中关于 `openBy` 的逻辑
- 简化 `dictationButtonHandler`，移除 `openBy` 设置

### 保留功能
- 默写模式开关（点击按钮或 Ctrl+V 快捷键）
- 全部隐藏模式
- Hover 显示答案功能（通过 `isShowAnswerOnHover` 配置）
- 下一个单词在默写模式下显示为下划线

### 移除功能
- 隐藏元音模式
- 隐藏辅音模式
- 随机隐藏模式
- 自动/手动打开区分逻辑
