# E2E 测试方案文档

## 一、测试目标

验证单词学习应用的核心业务流程：
- 词库创建是否正常
- 背单词流程是否正常
- 学习记录是否正确存储

---

## 二、测试流程设计

### 测试 1：词库创建流程

```
步骤流程：
┌─────────────────────────────────────────┐
│  1. 清空所有数据                          │
│     ↓                                    │
│  2. 访问词库管理页面                      │
│     ↓                                    │
│  3. 创建测试词库                          │
│     ↓                                    │
│  4. 验证词库列表显示                      │
│     ↓                                    │
│  5. 验证词库单词数量                      │
│     ↓                                    │
│  6. 验证单词数据结构                      │
└─────────────────────────────────────────┘

验证点：
├─ 词库列表：新词库出现在列表中
├─ 单词数量：与创建时指定的数量一致
├─ 单词数据：每个单词有 name 和 trans 字段
└─ 词库可选择：点击后可设置为当前词库
```

---

### 测试 2：背单词完整流程

```
步骤流程：
┌─────────────────────────────────────────┐
│  1. 清空所有数据                          │
│     ↓                                    │
│  2. 创建词库 [apple, banana]              │
│     ↓                                    │
│  3. 选择词库                              │
│     ↓                                    │
│  4. 进入学习页面                          │
│     ↓                                    │
│  5. 验证单词显示                          │
│     ↓                                    │
│  6. 验证释义显示                          │
│     ↓                                    │
│  7. 验证学习类型显示                      │
│     ↓                                    │
│  8. 输入第一个单词 apple                  │
│     ↓                                    │
│  9. 验证 masteryLevel = 1                │
│     ↓                                    │
│  10. 验证 learnedCount = 1               │
│     ↓                                    │
│  11. 验证自动切换到 banana                │
│     ↓                                    │
│  12. 输入第二个单词 banana                │
│     ↓                                    │
│  13. 验证 masteryLevel = 1               │
│     ↓                                    │
│  14. 验证 learnedCount = 2               │
│     ↓                                    │
│  15. 验证显示"学习完成"                   │
└─────────────────────────────────────────┘

验证点：
├─ 单词显示：word-name 元素内容正确
├─ 释义显示：translation 元素内容正确
├─ 学习类型：新词显示 "📚 新词"
├─ masteryLevel：学习后从 0 变为 1
├─ learnedCount：每学一个单词增加 1
├─ wordTypes：记录为 "new"
├─ 单词切换：完成后自动显示下一个
└─ 学习完成：全部完成后显示提示
```

---

### 测试 3：复习单词流程（第二天）

```
步骤流程：
┌─────────────────────────────────────────┐
│  === 第一天 ===                          │
│                                          │
│  1. 清空所有数据                          │
│     ↓                                    │
│  2. 创建词库 [reviewword]                 │
│     ↓                                    │
│  3. 选择词库                              │
│     ↓                                    │
│  4. 进入学习页面                          │
│     ↓                                    │
│  5. 输入单词 reviewword                   │
│     ↓                                    │
│  6. 验证 masteryLevel = 1                │
│     ↓                                    │
│  7. 验证 wordTypes = "new"               │
│     ↓                                    │
│  8. 验证 learnedCount = 1                │
│                                          │
│  === 推进时间到第二天 ===                 │
│  （nextReviewTime 到期）                  │
│                                          │
│  === 第二天 ===                          │
│                                          │
│  9. 进入学习页面                          │
│     ↓                                    │
│  10. 验证单词显示                         │
│     ↓                                    │
│  11. 验证学习类型显示 "🔄 复习"           │
│     ↓                                    │
│  12. 输入单词 reviewword                  │
│     ↓                                    │
│  13. 验证 masteryLevel = 2               │
│     ↓                                    │
│  14. 验证 wordTypes = "review"           │
│     ↓                                    │
│  15. 验证 reviewedCount = 1              │
└─────────────────────────────────────────┘

验证点：
├─ 第一天 masteryLevel：0 → 1
├─ 第一天 wordTypes："new"
├─ 第二天学习类型：显示 "🔄 复习"
├─ 第二天 masteryLevel：1 → 2
├─ 第二天 wordTypes："review"
└─ reviewedCount：复习计数增加
```

---

### 测试 4：新手每日学习上限

```
测试目标：验证新手一天能学习的单词数量上限

步骤流程：
┌─────────────────────────────────────────┐
│  1. 清空所有数据                          │
│     ↓                                    │
│  2. 创建词库（含30个单词）                 │
│     ↓                                    │
│  3. 验证每日上限配置（默认20）             │
│     ↓                                    │
│  4. 进入学习页面                          │
│     ↓                                    │
│  5. 学习单词直到上限                       │
│     ↓                                    │
│  6. 验证 learnedCount = 20               │
│     ↓                                    │
│  7. 验证显示"学习完成"                    │
│     ↓                                    │
│  8. 验证第21个单词不出现                  │
└─────────────────────────────────────────┘

验证点：
├─ 每日上限：默认为20
├─ learnedCount：达到上限值
├─ 学习完成：达到上限后显示提示
└─ 不超限：不能继续学习更多单词
```

---

## 三、测试优先级

```
P0（上线必须通过）：
├─ 测试 1：词库创建流程
├─ 测试 2：背单词完整流程
└─ 测试 3：复习单词流程

P1（重要功能）：
├─ 测试 4：每日学习上限
├─ 测试 5：错误输入处理
└─ 测试 6：掌握按钮功能

P2（次要功能）：
├─ 测试 7：页面刷新数据恢复
└─ 测试 8：空词库引导
```

---

## 四、核心验证规则

```
规则 1：每个测试从清空数据开始
规则 2：不能跳过任何步骤
规则 3：验证精确值，不模糊断言
规则 4：复习流程必须先跑第一天学习
规则 5：第二天场景必须推进时间
```

---

## 五、测试工具函数需求

```
需要提供的工具函数：

1. createTestDictionary(page, name, words)
   - 创建测试词库
   - 返回词库 ID

2. setCurrentDictionary(page, dictId)
   - 设置当前使用的词库

3. typeWord(page, word)
   - 输入单词
   - 验证输入完成

4. getWordProgress(page, dictId, word)
   - 获取单词 masteryLevel

5. getDailyRecord(page, dictId)
   - 获取今日学习记录

6. advanceToNextDay(page)
   - 推进时间到下一天

7. verifyWordDisplay(page, expectedWord)
   - 验证当前显示的单词

8. verifyLearningType(page, expectedType)
   - 验证学习类型显示（new/review）
```

---

## 六、测试执行顺序

```
执行顺序（按优先级）：

1. 先执行 P0 测试
   └─ 词库创建 → 背单词 → 复习

2. P0 全部通过后执行 P1
   └─ 每日上限 → 错误输入 → 掌握按钮

3. P1 全部通过后执行 P2
   └─ 刷新恢复 → 空词库

4. 所有测试通过 → 可以上线
```

---

## 七、测试环境要求

```
环境配置：
├─ Playwright 测试框架
├─ Chromium 浏览器
├─ 本地开发服务器 (localhost:8080)
└─ uTools mock 环境
```

---

## 八、测试有效性验证方法

### 为什么需要验证测试有效性

```
问题：测试通过不代表测试正确

场景 1：断言太弱
├─ expect(count).toBeGreaterThanOrEqual(1)
├─ 实际期望是 3，但断言只要求 ≥1
└─ 测试永远通过，无法发现问题

场景 2：假断言
├─ expect(data).toBe(expected)  写了断言
├─ 但代码逻辑错误，断言从未执行
└─ 测试永远显示通过

场景 3：数据错误但断言凑巧匹配
├─ 实际返回 1，期望也是 1
├─ 但数据来源是错误逻辑
└─ 测试通过，但功能有问题
```

---

### 验证方法：反向验证（制造 bug）

```
核心思路：
┌─────────────────────────────────────────────────────────────┐
│  如果测试真的能发现问题，那么当我制造 bug 时：                │
│  └─ 测试必须失败                                             │
│                                                             │
│  如果制造 bug 后测试仍然通过：                                │
│  └─ 说明测试无效                                             │
│  └─ 需要重新编写测试                                         │
└─────────────────────────────────────────────────────────────┘
```

---

### 验证步骤

```
步骤 1：修改业务代码，制造 bug

步骤 2：运行测试

步骤 3：检查结果
├─ 如果测试失败 → 测试有效 ✅
├─ 如果测试通过 → 测试无效 ❌

步骤 4：记录失败信息

步骤 5：恢复业务代码

步骤 6：运行测试确认通过
```

---

### 已验证的测试案例

#### 案例 1：验证 masteryLevel 断言有效

```
验证日期：2024-04-25
验证测试：测试 3 复习单词流程
验证方法：修改业务代码
制造 bug：
├─ 原代码：progress.masteryLevel + 1
├─ 修改为：progress.masteryLevel + 0
测试结果：
├─ Expected: 1
├─ Received: 0
├─ 状态：失败 ✅
结论：测试有效
```

#### 案例 2：验证 wordTypes 断言有效

```
验证日期：2024-04-25
验证测试：测试 3 复习单词流程
验证方法：修改断言期望值
制造 bug：
├─ 原断言：expect(wordTypes).toBe('new')
├─ 修改为：expect(wordTypes).toBe('wrong_value')
测试结果：
├─ Expected: 'wrong_value'
├─ Received: 'new'
├─ 状态：失败 ✅
结论：测试有效
```

#### 案例 3：验证每日上限断言有效

```
验证日期：2024-04-25
验证测试：测试 4 新手每日学习上限
验证方法：修改业务代码
制造 bug：
├─ 原代码：getDailyLimit() 返回配置值
├─ 修改为：return 999（忽略上限）
测试结果：
├─ Expected: null（期望没有更多单词）
├─ Received: "testf"（第6个单词出现）
├─ 状态：失败 ✅
结论：测试有效
```

#### 案例 4：测试发现了真实 bug

```
发现日期：2024-04-25
发现测试：测试 4 新手每日学习上限

问题描述：
├─ dailyLimitConfigAtom 配置存在
├─ 但 normalStrategy.getWordNames 硬编码上限为 20
├─ 没有读取用户配置的上限
└─ 每日上限功能没有真正生效

修复方案：
├─ 添加 getDailyLimit() 函数读取配置
├─ 修改 normalStrategy 使用配置上限
├─ 考虑已学习数量计算剩余配额
```

---

### 每个新测试必须执行的验证流程

```
┌─────────────────────────────────────────────────────────────┐
│  新测试编写完成后：                                          │
│                                                             │
│  1. 先运行测试确认通过                                       │
│                                                             │
│  2. 执行反向验证                                             │
│     ├─ 方法 A：修改业务代码制造 bug                          │
│     ├─ 方法 B：修改断言期望值为错误值                        │
│                                                             │
│  3. 运行测试，必须失败                                       │
│     ├─ 如果不失败 → 测试无效，重写                           │
│     ├─ 如果失败 → 记录失败信息                               │
│                                                             │
│  4. 恢复代码                                                 │
│                                                             │
│  5. 再次运行测试确认通过                                     │
│                                                             │
│  6. 在文档中记录验证结果                                     │
└─────────────────────────────────────────────────────────────┘
```

---

### 验证记录模板

```
每次验证必须记录：

验证日期：YYYY-MM-DD
验证测试：测试名称
验证方法：修改业务代码 / 修改断言期望值
制造 bug：
├─ 原代码：xxx
├─ 修改为：xxx
测试结果：
├─ Expected: xxx
├─ Received: xxx
├─ 状态：失败 ✅ / 通过 ❌
结论：测试有效 / 测试无效需重写
```

---

## 九、文档更新记录

```
创建时间：2024-04-25
创建人：Claude
状态：已审核

更新记录：
├─ 2024-04-25：添加测试有效性验证方法
├─ 2024-04-25：验证 masteryLevel 断言有效
├─ 2024-04-25：验证 wordTypes 断言有效
└─ 2024-04-25：P0 测试全部通过并验证有效
```